name: Linting

on:
  push:
    branches-ignore:
      - 'main'
jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - uses: cachix/install-nix-action@v22
      - run: nix flake check
